datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
  output   = "../external-db/generated"
}

model Example {
  id   Int    @id @default(autoincrement())
  name String
}
